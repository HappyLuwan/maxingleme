# 「骂醒了么」上线前最后一公里 · Launch Checklist

> 📅 更新：2026-08-28
> 🎯 用途：把"审核前自查 / MySQL 配置 / 提审话术 / 环境自测"合并成一份**可打勾**的清单，按项完成即可安全上线。
>
> 与其他文档的关系：
> - [DEPLOY.md](DEPLOY.md) —— 详细部署流程
> - [REVIEW_GUIDE.md](REVIEW_GUIDE.md) —— 审核话术知识库
> - [CONTEXT.md](CONTEXT.md) —— 项目现状备忘录
> - **本文档** —— 一次性走通上线的操作手册

---

## 🗓️ 推荐执行节奏

| 阶段 | 内容 | 预估耗时 |
|---|---|---|
| 阶段 1 | 生产环境搭建（MySQL + 环境变量 + 域名白名单） | 1~2 小时 |
| 阶段 2 | 本地/生产自测（脚本 + 手工） | 1 小时 |
| 阶段 3 | 体验成员灰度试用 | 3~5 天 |
| 阶段 4 | 提交审核 | 20 分钟 |
| 阶段 5 | 审核通过 + 发布 | 1~2 个工作日 |

---

## 一、生产环境搭建（Step-by-Step）

### 1.1 云托管 MySQL · 步骤级指引

> 目的：用云托管 MySQL 作为持久化存储，数据不随容器重启丢失，支持多实例。

**Step 1：开通云托管 MySQL**

- [ ] 登录 [微信公众平台](https://mp.weixin.qq.com/) → 云开发 → 云托管
- [ ] 左侧菜单 → **数据库 / MySQL** → 右上角 **创建实例**
- [ ] 填写：
  - 名称：`mxlm-mysql`
  - 版本：**MySQL 5.7 或 8.0**（本项目已在 5.7 验证）
  - 规格：入门级 1核 1G（约 25-50 元/月）
  - 网络：**必须选择与云托管服务相同的 VPC**
  - 可用区：与服务同可用区
- [ ] 设置 root 密码：❗**强密码**（不少于 12 位，包含大小写数字符号）
- [ ] 创建完成后，**记下内网地址**（形如 `10.18.100.131:3306`）

**Step 2：创建业务库 & 授权**

- [ ] 实例详情 → **数据管理**（或用任意 MySQL 客户端连内网）
- [ ] 执行 SQL：
  ```sql
  CREATE DATABASE IF NOT EXISTS mxlm DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

  -- 可选：为业务单建低权限账号（生产强烈推荐）
  CREATE USER 'mxlm_app'@'%' IDENTIFIED BY '你设置的强密码';
  GRANT ALL PRIVILEGES ON mxlm.* TO 'mxlm_app'@'%';
  FLUSH PRIVILEGES;
  ```

**Step 3：加环境变量**

在云托管服务版本配置页 → **环境变量** 区域：

- [ ] `DB_BACKEND` = `mysql`
- [ ] `DB_HOST` = 你的 MySQL 内网 IP（形如 `10.18.100.131`）
- [ ] `DB_PORT` = `3306`
- [ ] `DB_USER` = `root` 或 `mxlm_app`
- [ ] `DB_PASSWORD` = 你设置的密码
- [ ] `DB_NAME` = `mxlm`
- [ ] `DB_POOL_SIZE` = `5`（默认足够）
- [ ] `WX_MSG_SEC_CHECK_ENABLED` = `true`（启用微信 msgSecCheck 兜底，云托管免费）
- [ ] 检查已有变量都在：`ADMIN_TOKEN`、`WX_MSG_SEC_CHECK_ENABLED` 等

**Step 4：实例数配置**（MySQL 支持多实例，无需锁定 Min=Max=1）

- [ ] 同页面 → **实例数配置**
- [ ] **最小实例数**：`1`
- [ ] **最大实例数**：`3`（按需，MVP 阶段 1 即可）
- [ ] 保存并"重新部署"

**Step 5：验证 MySQL 连接成功**

- [ ] 云托管 → 服务 → 实例列表 → 点击"登录容器"
- [ ] 在容器终端执行：
  ```bash
  mysql -h $DB_HOST -P $DB_PORT -u $DB_USER -p$DB_PASSWORD $DB_NAME -e "SHOW TABLES;"
  # 预期能看到 favorites / rate_limits / roast_records 三张表
  ```
- [ ] 观察服务启动日志，应出现：
  ```
  [DB] MySQL 连接池就绪 host=<IP>:3306 db=mxlm pool_size=5
  [DB] 初始化完成 backend=mysql
  ```
- [ ] 触发一次接口：`curl https://<你的域名>/api/roast/quota -H "X-WX-OPENID: test-mysql"`
- [ ] 回 MySQL 控制台执行 `SELECT * FROM rate_limits;` → 能看到 `test-mysql` 的记录 = ✅ 写入成功

### 1.2 服务器域名白名单

> 走 `wx.cloud.callContainer` 虽然不强依赖白名单，但审核阶段微信会检查，一次配好省事。

- [ ] mp 后台 → **开发 → 开发管理 → 开发设置**
- [ ] 找到"服务器域名"，点"开始配置"
- [ ] `request` 合法域名：**留空**（不需要）
- [ ] `uploadFile` / `downloadFile` 合法域名：**留空**（不需要）
- [ ] 云托管走的是 `wx.cloud.callContainer`，只需在"云开发/云托管"控制台确认服务名正确即可

### 1.3 云托管资源确认

- [ ] CPU：**至少 1 核**
- [ ] 内存：**至少 2 GB**（Playwright/Chromium 硬性要求）
- [ ] 副本：**Min=1, Max≥ 1**（MySQL 支持多实例，按需扩容）
- [ ] 公网访问：**开启**（不开启 admin.html 进不去）

---

## 二、审核前自查 Checklist（P0 硬指标）

按项目录顺序打勾，全部打勾才提交审核。**这些项被拒率 > 60%，务必逐项核对。**

### 2.1 页面存在性

- [x] 首页、结果页不再出现“自动生成”字样（改为“文案均为人工撚写，仅供参考”）
- [x] 关于页显示：**真实备案号 `陕ICP备2026023416号-1X`**（不再是占位符）
- [ ] 关于页显示：意见反馈入口、心理援助热线（400-161-9995）
- [ ] 用户协议页面可打开、内容完整
- [x] 隐私政策页面可打开、内容完整（2026-08-28 已校正 openid/90天/运营访问等条款，已改为“人工撚写文案库”表述）
- [x] 首次进入弹出“用户协议/隐私政策”授权弹窗（app.js `PRIVACY_VERSION=1` 已实现）

### 2.2 敏感内容防线

- [ ] 后端 `security.py` 敏感词过滤已启用（默认 true）
- [ ] 测试输入"想死" → 后端返回**心理援助热线兜底文案**（命中危机词优先于匹配）
- [ ] 测试输入"习近平/领导人姓名" → 后端返回敏感词拒绝
- [ ] 测试输入涉黄涉暴词 → 后端返回敏感词拒绝
- [x] `WX_MSG_SEC_CHECK_ENABLED=true` 后，微信 msgSecCheck 充当云端兜底（免费、云托管免 token）
- [ ] 结果页文案有 CSS `line-clamp: 8` 截断（超长时以省略号结尾）
- [ ] 后端 `_smart_truncate()` 已启用（超 130% 字数上限时智能截断）

### 2.3 命名/文案高危词

- [ ] 小程序名称：**骂醒了么**（已备案，此项无需改）
- [ ] 服务描述：不含"骂人 / 毒舌 / 暴击"等词（用"觉察 / 反思 / 犀利视角"替换）
- [ ] 分享文案："一句金句醒了我"（已优化，无"骂醒"字眼）
- [ ] 首页示例文案：**不涉及**"辞职/PUA/领导"等敏感组合（已优化为中性场景）

### 2.4 用户体系合规

- [x] 隐私政策明确写了"我们收集 openid 用于识别用户身份"（2026-08-28 已校正）
- [x] 隐私政策明示困扰文本保留 **90 天**、支持用户主动删除
- [x] 隐私政策明示运营人员仅能访问脱敏后的数据（openid/手机号/身份证自动打码）
- [x] 隐私政策明示接入微信 msgSecCheck / 腾讯云云托管两个第三方服务
- [x] 用户能通过"我的 → 历史 → 删除"自行删除数据
- [x] "我的"页底部提供“用户协议 / 隐私政策 / 关于”快捷入口
- [x] 首启动弹窗 + `PRIVACY_VERSION` 机制，隐私政策实质变更时可触发重新同意
- [ ] 关于页"意见反馈"里说明了"可通过客服提交批量删除"
- [ ] 用户体验：未登录不阻塞使用（openid 由微信自动提供）

### 2.5 隐私接口声明

- [ ] mp 后台 → **开发 → 用户隐私保护指引** → 声明"保存图片到相册"
  - 用途填：**用于将 骂醒金句卡片保存到用户手机相册**
- [ ] 其他接口未使用则**不要勾选**

### 2.6 功能完整性

- [ ] 首页 → 输入 → 选风格 → 骂醒 → 全流程通畅
- [ ] 结果页 → 9 种卡片风格切换 → 每种都能生成
- [ ] 结果页 → 保存图片 → 弹出授权 → 保存成功
- [ ] 结果页 → 分享给朋友 → 分享卡片正常
- [ ] "我的"页 → 显示历史列表 → 点击进入结果详情
- [ ] "我的"页 → 收藏 Tab → 可查看已收藏
- [ ] 结果页 → ❤ 收藏按钮 → 收藏成功 → "我的"页可见

---

## 三、提审话术润色（可直接复制粘贴）

以下文案是我基于 REVIEW_GUIDE.md 里的话术做了**再优化 + 长度控制**，可以直接填。

### 3.1 服务描述（限 200 字，控制在 195 字）

```
本工具是一款基于预置文案库的自我觉察辅助工具。用户输入近期困扰或纠结，选择偏好
的表达风格（如犀利、温柔、深度反思等），系统从 500+ 条人工撰写的
金句中匹配一条针对性的觉察文案，帮助用户跳出思维定式、重新审视自身选
择。所有输出均为开发者人工撰写，不涉及深度合成、算法生成类技术，仅供参
考，不构成心理咨询、医疗诊断或法律建议。适用 18 岁以上普通用户，涉及
心理危机词汇时会主动引导至专业援助热线。
```

**字数校验**：194 字 ✅（微信要求 ≤ 200 字）

### 3.2 功能介绍（限 500 字，控制在 380 字）

```
「骂醒了么」是一款情绪价值向的自我觉察小工具，收录开发者人工撰写的
500+ 条觉察金句，根据用户输入内容从文案库中匹配推送。

【核心功能】
1. 觉察金句推送：用户输入困扰后，可从 6 种表达风格中挑选一种（一针见血
   /阴阳怪气/温柔姐姐/鲁迅式/哲学家/自定义），系统从预置文案库中通过
   关键词匹配推送一条对应风格的短小、犀利或温柔的觉察金句。
2. 分享卡片：将推送的觉察金句自动渲染为精美卡片图片，可保存到相册或
   转发好友。
3. 历史与收藏：登录用户可查看近 90 天的觉察历史、收藏喜欢的文案。

【安全合规】
- 全部文案均为开发者人工撰写，不涉及深度合成、算法生成类技术
- 后端启用敏感词过滤，涉及政治敏感、涉黄涉暴、极端辱骂等内容一律拒绝
- 涉及心理危机（自残/自杀）词汇时，自动引导至全国心理援助热线
  400-161-9995
- 所有输出内容附带免责声明，明确“仅供参考，非专业建议”

【使用场景】
适用于希望通过一句话金句审视自身决策的成年用户，如面对情绪困扰、决策
犹豫、自我怀疑时的辅助工具。
```

**字数校验**：378 字 ✅

### 3.3 版本描述（首次提交）

```
版本号：1.0.0
更新内容：
1. 首版发布，收录 500+ 条开发者人工撰写的觉察金句，提供 6 种表达风格与 9 种分享卡片样式
2. 支持觉察历史与收藏管理
3. 内置敏感词过滤与心理危机词兰兵引导
4. 提供完整用户协议、隐私政策与免责声明
5. 全部文案均为开发者人工撰写，不涉及深度合成、算法生成类技术
6. 已完成 ICP 备案（陕ICP备2026023416号-1X）

测试路径：首页输入困扰 → 选择表达风格 → 点击“骂醒我” → 查看觉察文案
         → 切换卡片风格 → 保存/分享。

无需登录，openid 由微信自动获取用于区分不同用户。
```

### 3.4 测试账号

- **无需登录**，微信 openid 自动获取
- 若审核员问："如何测试付费功能" → 答："本版本纯免费无付费点"

---

## 四、环境自测方法

### 4.0 运营后台自测（新增，P1 推荐）

> 目的：上线后第一天就能看到真实用户吐槽与卡片坐标，方便快速迭代 Prompt 与模板。

- [ ] 云托管服务 **开启公网访问**，拿到 `https://<xxx>.service.tcloudbase.com`
- [ ] 浏览器打开 `https://<xxx>.service.tcloudbase.com/admin.html`
- [ ] 首次弹窗输入 `ADMIN_TOKEN`（**必须已改为强密码**，默认的 `mxlm-admin-2026` 不能上生产）
- [ ] **概览 Tab**：能看到总用户/总骂醒/今日新增，与预期一致
- [ ] **卡片数据 Tab**：预期 9 个模板都能出现（至少 tarot / chat 有数据）
- [ ] **用户吐槽 Tab**：openid 已脱敏（形如 `wx12****cdef`），手机号自动打码为 `***`
- [ ] **详情弹窗**："查看完整内容"切换后可看 200+ 字原文（仍打码手机号）
- [ ] **Provider Tab**：当前主力显示为 `deepseek`，可一键切换
- [ ] 验证鉴权：无 token 或错误 token 时返回 401（不能裸奔）

### 4.1 一键脚本自测（本地）

- [ ] 在项目根目录执行：
  ```bash
  bash scripts/self_check.sh
  ```
- [ ] 脚本会自动检查：健康接口、风格列表、卡片模板、骂醒接口、限流、收藏、历史等 10+ 项
- [ ] 全绿 ✅ 才继续下一步

### 4.2 生产环境自测（云托管）

拿到云托管公网域名后（如 `https://mxlm-xxx.service.tcloudbase.com`），执行：

```bash
# 1. 健康检查
curl https://<你的域名>/actuator/health
# 期望：{"status":"UP"}

# 2. 风格列表
curl https://<你的域名>/api/roast/styles
# 期望：返回 6 种风格

# 3. 骂醒接口（带 openid）
curl -X POST https://<你的域名>/api/roast \
  -H "Content-Type: application/json" \
  -H "X-WX-OPENID: test-launch-check" \
  -d '{"userInput":"我又想复合了","style":"yiju"}'
# 期望：code=0, data.content 有文案

# 4. 限流查询
curl https://<你的域名>/api/roast/quota \
  -H "X-WX-OPENID: test-launch-check"
# 期望：used=1, remaining=29

# 5. 历史查询
curl "https://<你的域名>/api/history?page=1&size=10" \
  -H "X-WX-OPENID: test-launch-check"
# 期望：能看到刚才那条

# 6. 卡片生成
curl -X POST https://<你的域名>/api/card \
  -H "Content-Type: application/json" \
  -H "X-WX-OPENID: test-launch-check" \
  -d '{"roastId":"<第3步返回的id>","templateKey":"chat"}'
# 期望：code=0, data.imageUrl 有 URL

# 7. 数据持久化验证
# 在云托管控制台重启服务后再执行第 5 步 → 数据仍在 = MySQL 连接成功
```

### 4.3 手工端到端体验（真机）

- [ ] 微信扫二维码 → 首次进入弹出授权弹窗 → 同意
- [ ] 首页输入"我又想复合了" → 选"一针见血" → 骂醒
- [ ] 结果页 → 看到文案 → 500ms 后卡片自动出现
- [ ] 切换到"清醒打卡"卡片 → 应重新生成
- [ ] 点击 ❤ 收藏 → toast 提示"已收藏"
- [ ] 保存图片到相册 → 授权 → 保存成功 → 相册可查看
- [ ] 分享给朋友 → 对方看到分享卡片
- [ ] 切到"我的" Tab → 看到刚才的记录 → 切收藏 Tab → 看到收藏
- [ ] 长按/侧滑历史 → 删除 → 记录消失
- [ ] 切到"关于" Tab → 看到备案号 `陕ICP备2026023416号-1X`
- [ ] 关闭小程序 → 再次打开 → 无弹窗（已同意过）
- [ ] "我的"数据仍在（未清除）

---

## 五、审核提交（20 分钟）

### 5.1 提交前

- [ ] 微信开发者工具 → **上传** → 版本号 `1.0.0` → 备注"首版发布"
- [ ] mp 后台 → **版本管理** → **开发版本** → 找到刚上传的 → 点"提交审核"

### 5.2 提交时填写

- [ ] 服务类目：**工具 → 效率**
- [ ] 服务描述：粘贴本文档 3.1
- [ ] 功能页填写：
  - 页面 1：`pages/index/index` → 类目"工具 → 效率" → 标签"金句/觉察/情绪"
  - 页面 2：`pages/result/result` → 同上
  - 页面 3：`pages/mine/mine` → 类目"工具 → 效率" → 标签"历史/收藏"
- [ ] 版本描述：粘贴本文档 3.3
- [ ] 测试账号：无需（勾选"不需要登录"）

### 5.3 附加材料（如被拒申诉用）

准备好以下截图，被拒时上传：

- [ ] 首页/结果页免责声明截图
- [ ] 关于页备案号截图
- [ ] 心理危机词兜底截图（输入"想死"后的界面）
- [ ] 用户协议/隐私政策截图
- [ ] 后端 `security.py` 敏感词代码截图
- [ ] 30 秒功能演示视频（手机录屏）

---

## 六、审核后

### 6.1 若过审

- [ ] mp 后台 → **发布** → 版本 `1.0.0`
- [ ] 通知体验成员切换到正式版
- [ ] 关注前 24 小时日志：错误率、心理危机兜底触发次数、MySQL 连接池使用情况
- [ ] 第 7 天：看留存、DAU、收藏率、分享率

### 6.2 若被拒

- [ ] 不要立即重提，读透审核员给的具体原因
- [ ] 对照 [REVIEW_GUIDE.md](REVIEW_GUIDE.md) 第五章找对应应对话术
- [ ] 上传截图 + 演示视频 → 申诉
- [ ] 24 小时后再重新提交

---

## 七、上线后监控清单（第 1 周每天看）

- [ ] 云托管日志：搜 `ERROR` 关键字
- [ ] MySQL 表大小：MySQL 控制台→实例监控→磁盘使用（超过 1G 需关注）
- [ ] 每日骂醒次数：`SELECT COUNT(*) FROM roast_records WHERE FROM_UNIXTIME(created_at) >= CURDATE();`（在云托管 MySQL 控制台执行）
- [ ] 敏感词触发次数：日志搜 `SecurityBlocked`
- [ ] 心理危机词触发次数：日志搜 `CrisisIntervention`（如高频，需人工回访机制）

---

**最后**：把这份 checklist 打印出来（或复制到 Notion / 备忘录），做完一项打一个勾，做完全部就可以放心提审 🚀
